name: Deploy to Railway

# Trigger deployment on push to main branch
on:
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual trigger from GitHub Actions tab

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests (if any)
        run: |
          # Add your test commands here
          # python -m pytest tests/
          echo "No tests configured yet"

      - name: Deploy to Railway
        run: |
          echo "Railway auto-deploys from GitHub"
          echo "No manual deployment step needed"
          echo "Check Railway dashboard for deployment status"

      - name: Deployment Info
        run: |
          echo "✅ Code pushed to main branch"
          echo "🚂 Railway will auto-deploy in ~5 minutes"
          echo "📊 Check: https://railway.com/project/9b2e9d9a-97e6-4dff-983f-a3704e680083"
